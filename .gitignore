node_modules/
.data/
public/
dist/
*.log
*.tsbuildinfo
.env
.env.*
.DS_Store
# Run markers: the tool step exits 0 so its commit step still runs, and a trailing
# `if: always()` step reds the job from these. Never commit them.
archive-blocked.txt
backfill-blocked.txt
